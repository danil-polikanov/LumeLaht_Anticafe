global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'monolith'
    metrics_path: /metrics
    static_configs:
      - targets: ['app:8080']
        labels:
          architecture: 'monolith'
